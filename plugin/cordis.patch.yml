# dsh-sub-cli bundle patch: one host plugin row that registers the `dsh-sub-cli`
# settings section (unified dir + per-CLI model route) and provides:
#   - a `cli_dispatch` model tool that runs an installed external Agent CLI headless
#   - Model tools (cli_codex / cli_claude_code / cli_opencode / cli_gemini) that
#     delegate through managed SubagentProviders
#   - a `cli_check` model tool that reports install/version per CLI
#   - a remote `cli` service with check / test / remove operations
# so the Web panel (settings section) stays in sync with real installed state.
- insert:
    - id: dsh-sub-cli
      name: 'dsh-sub-cli'
