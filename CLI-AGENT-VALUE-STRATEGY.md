# dsh-sub-cli：核心价值评估与许可证策略（2026-09-01 · 修订）

> **背景**：功能已跑通后，需回答两个战略问题：
> 1. 我们的工作是否有**核心价值**？
> 2. 如果有，是否要**改变 MIT 协议**？
>
> **重大澄清（本版修订）**：`dsh-sub-cli` 只提供**基础能力层**；**编排行动（更高价值部分）会放到同项目的 `dsh-ldvh`（LDVH v5 仓库）里实现**。
> 这直接改变了「核心价值」的归属与许可证结论。
>
> 状态：**分析结论，待你决策**。许可证变更属重大决定，需 Human Gate。
> 配套参考：`CLI-AGENT-VALUE-RESEARCH.md`（赛道/更高价值方向调研）。

---

## 1. 结论摘要（修订后）

- **有核心价值，但价值的最终落点不在本插件，而在编排层（LDVH）**。
- 本插件的价值是**通用的「外部 Agent CLI 编排底座」**，对任何想要编排 CLI 的项目都有用。
- **两个仓库都应维持 MIT，不需要改变协议**：
  - `dsh-sub-cli`：MIT（基础能力，保持最大采用/传播杠杆）；
  - `dsh-ldvh`：MIT（已明确定义，与 DSH 本体同协议、接受开源公共契约）。

---

## 2. 分层定位（关键）

```
┌─────────────────────────────────────────────┐
│  编排行动层  →  dsh-ldvh（LDVH v5）          │
│  · 把任务拆给多个 CLI / 并行编排              │
│  · 跨供应商模型路由 / 故障转移 / 多模型仲裁     │
│  · 团队/企业控制面、审计、治理                │
│  · 具体「编排行为」与「规则」落地在这里         │
├─────────────────────────────────────────────┤
│  基础能力层  →  dsh-sub-cli（本插件）          │
│  · 统一安装 / 隔离配置 / 权限 / 供应商指纹     │
│  · 把 Codex/Claude Code/Qwen 作为原生子代理    │
│  · 每 CLI 的 Provider→模型→推理强度 路由        │
│  · 通用的「被主控调用」接口                    │
└─────────────────────────────────────────────┘
```

- **本插件** = 基础设施，谁都能用，谁都能编排。
- **LDVH** = 消费这个基础设施去做「更高价值」的编排（LDVH 的核心是「让 AI 开发稳定可控」，编排多个 CLI 是其中一个动作）。

---

## 3. 核心价值：本插件的价值到底在哪

### 3.1 一句话价值主张

> **一个通用的、与 DSH 原生机制绑定的「外部 Agent CLI 编排底座」：统一安装、隔离配置、可路由模型、可配权限，让外部 CLI 像原生子代理一样被任意主控（含 LDVH）调用。**

### 3.2 价值锚点（外部调研佐证）

1. **踩在生态主流上（direct integration）**：唯一直接同类 `all-agents-mcp` 已归档，作者明说「direct integration has become the standard」。我们做的正是深度集成，方向正确且未被占满。
2. **竞品缺「控制面」**：Emdash / Pane 只解决「同时跑多个 agent」，缺统一安装/配置/权限/指纹。这些是我们在 `lib/` 已实现的。
3. **「互相充当子代理」是真实需求**：`claude-codex-subagent` 等都在做「一个 CLI 调另一个」——多为单向（Claude 调 Codex 省 token）。我们泛化为「任意 CLI ↔ 主控编排」。
4. **对 LDVH 的价值**：LDVH 要落地「让多个外部 CLI 稳定受控地参与开发」，本插件提供它需要的**受控底座**（权限、指纹、隔离、子代理接口）。

### 3.3 诚实的边界

- 我们不是「最强编码 agent」，是**它们的编排/管理底座**。
- 价值成立前提是「用户愿意用外部 Agent CLI」。
- **尚无付费验证、无用户规模**；「价值」是判断，未获市场证实。
- 编排层的价值属于 LDVH；本插件单独看价值偏「工具箱」。

---

## 4. 许可证现状

- `dsh-sub-cli/plugin/LICENSE`：**MIT**，Copyright (c) 2026 dingminhua；`package.json` `"license": "MIT"`。
- `dsh-ldvh`：**MIT**（仓库根 LICENSE），README 明确「与 DSH 本体同协议，接受开源公共契约，开源本意是让他人做移植」。

---

## 5. 竞品/同类许可证对比

| 项目 | 许可证 | 商业化 |
|---|---|---|
| **Pane / RunPane** | **AGPL-3.0** | 无付费层、无限制 |
| **Emdash**（YC W26）| 独立 LICENSE | YC 创业，有商业诉求 |
| **all-agents-mcp**（同类唯一）| 已归档 | 无 |
| **opencode**（anomalyco）| 开源编码 agent（20 万 star）| 大众开源 |
| **Sentry**（跨领域经典）| BSD-3 → BUSL → **FSL** | 防 harmful free-riding |

启示：
- Pane 用 AGPL-3.0 仍零商业化 → **收紧许可 ≠ 商业成功**，赛道在抢用户。
- Sentry 换许可的前提是有庞大的商业收入与用户基础。我们未到该阶段。

---

## 6. 是否改 MIT？——建议（修订后：不改）

### 6.1 现在不建议改（两个仓库都维持 MIT）

1. **没有用户、没有收入**，改保守许可只会减少传播与采用。
2. **竞品证据**：Pane 用 AGPL 仍免费，靠许可无法变现。
3. **分层后更不需要收紧**：本插件是基础层，保持 MIT 利于被 LDVH 及社区采用；LDVH 已明确 MIT，与 DSH 本体同协议，是既定的开源公共契约。

### 6.2 未来商业化的正确顺序（不是先改许可）

1. 保持 MIT，把「基础底座」做成有口碑的通用能力；
2. 增值/编排层（模型路由、并行编排、团队控制面、审计）天然属于 LDVH 或独立可售层；
3. 有真实付费用户后，再评估整体或部分切换到 FSL / BUSL / 双许可（参考 Sentry 路径）——且这主要是 LDVH 层的事，不是基础插件事。

### 6.3 需要立即做的事（不涉及协议变更）

- **接好分层**：把 dsh-sub-cli 接入 dsh-ldvh（当前 grep 显示 LDVH 尚未引用本插件），让「基础能力」真正被「编排层」消费。
- **价值文档化**：把 `CLI-AGENT-VALUE-RESEARCH.md` 的方向写进 README 作为未来路线。
- **token 成本实测**：产出「委派外部 CLI vs 主控直接做」量化对比，作为卖点证据。

---

## 7. 结论（修订后）

- **有核心价值**：本插件是「外部 Agent CLI 的通用编排底座」，最终价值由编排层（LDVH）放大。
- **不改 MIT**：两个仓库都维持 MIT。现在改是负收益；商业化的正确路径是「MIT 底座 + 可售编排层 + 用户验证后再说许可」。
- **关键动作**：先把 dsh-sub-cli 接入 dsh-ldvh，让基础能力被编排层真正消费；同时把价值方向文档化、产出 token 对比数据。
